<%= form_for @initiative do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :content %>

  <%= collection_select(:initiatives, :topic_id, Topic.all, :id, :description) %>

  <%= f.submit 'Create Initiative' %>
<% end %>